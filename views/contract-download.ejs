<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body { max-width: 52rem; margin: 0 auto; padding: 2rem 1.5rem; }
    .service-contract-table th { width: 11rem; white-space: nowrap; vertical-align: top; }
    .service-contract-body { white-space: pre-wrap; }
    .service-contract-signature-line { border-bottom: 1px solid #333; height: 2rem; }
    @media print { body { max-width: 100%; padding: 0; } }
  </style>
</head>
<body>
  <%- include('partials/service-contract-document', { event, contract, subcontractorLabel }) %>
</body>
</html>
