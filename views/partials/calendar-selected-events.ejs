<% if (selectedDate && selectedEvents.length > 0) { %>
  <div class="card">
    <div class="card-header bg-white py-3">
      <h2 class="h5 mb-0"><%= selectedEventsTitle %></h2>
    </div>
    <div class="list-group list-group-flush">
      <% selectedEvents.forEach(function(ev) { %>
        <div class="list-group-item py-3">
          <div class="d-flex justify-content-between align-items-center gap-2 flex-wrap">
            <div>
              <strong><%= clientShortName(ev) %></strong>
              <span class="text-muted mx-1">|</span>
              <%= ev.event_type %>
              <span class="text-muted mx-1">|</span>
              <%= ev.venue || '—' %>
              <span class="badge <%= statusBadgeClass(ev.status) %> ms-2"><%= statusLabel(ev.status) %></span>
            </div>
            <div class="d-flex gap-2 flex-wrap">
              <% if (typeof showContractLink !== 'undefined' && showContractLink && ev.contractUrl) { %>
                <a href="<%= ev.contractUrl %>" class="btn btn-primary btn-sm">Contrat <%= subcontractorLabel %></a>
              <% } %>
              <a href="/events/<%= ev.id %>" class="btn btn-outline-primary btn-sm"><%= openLabel || 'Ouvrir' %></a>
            </div>
          </div>
        </div>
      <% }); %>
    </div>
  </div>
<% } else if (selectedDate) { %>
  <div class="alert alert-light text-center">Aucun événement le <%= formatDateFr(selectedDate) %>.</div>
<% } %>
