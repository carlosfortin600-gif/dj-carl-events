<% const dayContact = typeof dayContactData !== 'undefined' ? dayContactData : { name: '', phone: '', email: '' }; %>
<% const venueContact = typeof venueContactData !== 'undefined' ? venueContactData : { name: '', phone: '', email: '', technical_fog_allowed: '' }; %>

<%- include('day-contact-fields', { dayContact }) %>
<%- include('venue-contact-fields', { venueContact, isPortal: typeof isPortal !== 'undefined' && isPortal }) %>
